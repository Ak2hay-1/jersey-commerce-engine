NEXT_PUBLIC_API_URL=http://localhost:4000
# admin = website CMS + promo codes (VM). erp = inventory/sales (local PC). all = both (local development).
NEXT_PUBLIC_PORTAL=all
NEXT_PUBLIC_DEFAULT_TENANT_SLUG=demo-jersey-store
