# Optional SSH password for the shared Cullinos + Jerzyfy VM (95.135.254.46).
# Copy to .cohost-ssh-password (gitignored) or set $env:COHOST_SSH_PASSWORD.
# Falls back to .vultr-ssh-password if unset.
